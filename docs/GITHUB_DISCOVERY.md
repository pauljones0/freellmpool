# Fork repository metadata

Repository: `pauljones0/freellmpool`. The maintained fork is installed from its reviewed source; inherited registry identities and historical assets do not represent a new package release.

Configured About description (90 chars):

> A maintained LLM gateway for legitimate free allowances, shared quotas, and coding agents.

Suggested topics:

`anthropic`, `claude`, `codex`, `cursor`, `failover`, `free-llm`,
`free-llm-api`, `gemini`, `groq`, `llm-gateway`, `llm-router`, `mcp`,
`mcp-server`, `model-context-protocol`, `openai`, `openai-proxy`,
`openrouter`, `python`, `rate-limiting`, `speech-to-text`.

Apply metadata to this fork only:

```sh
gh repo edit pauljones0/freellmpool \
  --description "A maintained LLM gateway for legitimate free allowances, shared quotas, and coding agents."
```

The description must match `pyproject.toml`; fixed catalog counts are reserved for the archived compatibility guides. Account evidence, live allowance totals and private operational results do not belong in public metadata.

## Historical assets

`assets/social-preview.svg` and `assets/social-preview.png` are retained from the compatibility release. They contain dated catalog claims; do not present them as the maintained gateway's current capacity. Any future replacement should be reviewed before upload. No custom social-preview upload is claimed here.

Pin `pauljones0/freellmpool` through the owner's GitHub profile if desired. Repository files cannot verify profile pinning.

Upstream source and MIT attribution remain [0xzr/freellmpool](https://github.com/0xzr/freellmpool). Inherited publishing workflows are restricted to that original repository and cannot publish this fork into its namespaces.
